<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="YsmStore.Controls.PasswordEntry">
  <ContentView.Content>
        <Frame x:Name="frame" Style="{StaticResource RoundedFrame}" Padding="15, 0">
            <Grid ColumnDefinitions="*, 30">
                <Entry x:Name="entry" IsPassword="True" Style="{StaticResource Entry1}"/>
                <Image x:Name="image" Source="eye_closed" Grid.Column="1">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="Image_Tapped"/>
                    </Image.GestureRecognizers>
                </Image>
            </Grid>
        </Frame>
    </ContentView.Content>
</ContentView>